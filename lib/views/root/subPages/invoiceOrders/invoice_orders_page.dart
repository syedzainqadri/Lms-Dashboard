import 'package:flutter/material.dart';


class InvoiceOrdersPage extends StatelessWidget {
  const InvoiceOrdersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepPurpleAccent,
      child: const Center(child: Text("Invoice Orders page to be developed")),
    );
  }
}